var inquirer = require('inquirer');
var game = require("./word");
var count = 0;

// find word function?

// enter letter function?
	// recursion function where it loops until count of blank spaces = 0